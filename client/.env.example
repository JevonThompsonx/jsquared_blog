# client/.env

# Supabase credentials for the client-side application.
VITE_SUPABASE_URL="YOUR_SUPABASE_PROJECT_URL"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# While Wrangler is used for deployment, its specific environment variables (e.g., CLOUDFLARE_API_TOKEN)
# are typically handled in CI/CD or global configurations, not usually in the client's .env.example.