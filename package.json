{
  "name": "jsquaredadventures",
  "version": "0.3.1",
  "description": "J² Adventures travel blog",
  "author": "Jevon Thompson",
  "license": "MIT",
  "homepage": "https://jsquaredadventures.com",
  "workspaces": [
    "./web"
  ],
  "scripts": {
    "dev": "cd web && bun run dev",
    "build": "cd web && bun run build",
    "lint": "cd web && bun run lint",
    "test": "cd web && bun run test"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
