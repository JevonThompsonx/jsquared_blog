# server/.env - Used by seed script
# DO NOT use quotes around values

SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# Note: For local development with wrangler, use .dev.vars instead
# See .dev.vars.example for the correct format
