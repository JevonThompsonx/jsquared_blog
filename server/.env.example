# server/.env

# Supabase credentials for the server-side Worker.
# These are picked up by Wrangler from wrangler.toml's [vars] section.
SUPABASE_URL="YOUR_SUPABASE_PROJECT_URL"
SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Wrangler typically uses environment variables for deployment (e.g., CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID),
# which are usually set in CI/CD or globally, not typically in .env.example for local development.